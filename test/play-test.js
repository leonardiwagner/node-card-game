var assert = require('chai').assert;
/*
require('../player.js');
require('../deck.js');
require('../card.js');
require('../play.js');

it('weaker card should lose for a stronger one', function(done){
  
  var player1 = new Player(0, "John", 0);
  var player2 = new Player(1, "Gabriel", 1);

  var weakCard = new Card(3,4);
  var strongCard = new Card(1,2);

  var play = new Play(null);

  play.registerMove(player1, weakCard);
  assert.equal(true , play.getWinningPlayer() === player1);
  assert.equal(true, play.getWinningCard() === weakCard);

  play.registerMove(player2, strongCard);
  assert.equal(true, play.getWinningPlayer()=== player2);
  assert.equal(true, play.getWinningCard()=== strongCard);

  done();
});

it('stronger card should win for a weaker one', function(done){
  
  var player1 = new Player(0, "John", 0);
  var player2 = new Player(1, "Gabriel", 1);

  var weakCard = new Card(3,4);
  var strongCard = new Card(1,2);

  var play = new Play(null);

  play.registerMove(player1, strongCard);
  assert.equal(true , play.getWinningPlayer() === player1);
  assert.equal(true, play.getWinningCard() === strongCard);

  play.registerMove(player2, weakCard);
  assert.equal(true, play.getWinningPlayer()=== player1);
  assert.equal(true, play.getWinningCard()=== strongCard);

  done();
});


*/